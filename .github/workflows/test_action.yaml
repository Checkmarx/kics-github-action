name: Test KICS action

on:
  pull_request:
    types: [opened]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Test KICS action
        uses: ./
        with:
          path: positive.tf
